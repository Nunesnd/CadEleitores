@model ProfissaoModel

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Lista de profissões";
}

<div class="text-center">
    <h1>Lista de profissões</h1>

    
                <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a class="btn btn-primary" role="button" asp-controller="Home" asp-action="Profissao">Nova profissao</a>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nome</th>
            </tr>
        </thead>
        <tbody>


            @*
            @if (Model != null Model())
            {
                foreach (ProfissaoModel profissao in Model)
                {
                    <tr>
                        <th scope="row">@profissao.Id</th>
                        <td>@profissao.NomeProfissao</td>
                    </tr>
                }
            }
           

            @foreach(var item in Model)
            {
                                    <tr>
                        <th scope="row">@item.Id</th>
                        <td>@item.NomeProfissao</td>
                    </tr>
            }
            *@
        </tbody>
    </table>
</div>